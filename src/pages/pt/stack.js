import React from 'react';

import Stack from 'pages/stack'; // eslint-disable-line
import pt from 'intl/data/pt';

const StackPt = props => (
  <Stack intl={pt} {...props} />
);

export default StackPt;
